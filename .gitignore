# Binaries and build output
/bin/
/out/
*.exe
/hush

# Web UI
web/node_modules/
web/dist/
# Embedded build output (produced by the Docker build; .gitkeep is kept)
internal/server/web/dist/*
!internal/server/web/dist/.gitkeep

# Local data and secrets
/data/
*.db
master.key
.env
